<h1>Stores#show</h1>
<p>お店の詳細紹介</p>

<div class="store-show">
	<div><%= attachment_image_tag @store, :image,:fill,400,400, fallback: "no_image.jpg",size:'400x400' %></div>
	<div><%= @store.store_name %></div>
	<div><%= @store.good %></div>
	<div><%= @store.introduction %></div>
	<% if customer_signed_in? %>
		<!-- ownerのidがほしい -->
		<%= form_tag("/conversations", method: "post") do %> <!-- conversation create にデータを送る -->
		<%= hidden_field_tag :owner_id, @store.owner.id%><!-- 使用者には見えないデータ（ここでいう@store.owner.idをowner_id → 名前はなんでもいい）を送る　 -->
		<%= submit_tag("メールする")%>
		<% end %>
	<% end %>
</div>
<!-- 店主の投稿一覧 -->
	<div class="post-box">
		<% @posts.each do |post|%>
		<div class="post-item">
			<div><%= attachment_image_tag post, :image, :fill,500,300,fallback:"no_image.jpg",size:'500x300'%></div>
			<span><%= post.comment %></span>
		</div>
		<% end %>
	</div>